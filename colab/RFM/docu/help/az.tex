\az:

Format: \stmt{az $<$clause$>$}

Options: $<$clause$>$ a \RELFUN{} clause

Effect: The $<$clause$>$ will be inserted at the end of the
        (possibly empty) \RELFUN{} database \stmt{*rfi-database*}.

see also: \aNull, \azhn, \azft, \consult, \destroy, \replace
