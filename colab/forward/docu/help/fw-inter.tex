\stmb{fw-inter}:

Format: \stmt{fw-inter}
  
Options: none

Effect: With this command you are leaving the emulator mode of \FORWARD{}, you return
	to the interpreter mode and the prompt changes to \stmt{fwi>}.

see also: \stmb{fw-emul} 

