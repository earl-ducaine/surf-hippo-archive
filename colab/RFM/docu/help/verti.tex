\verti:

Format: \stmt{verti $<$op$>$}

Options: $<$op$>$ an operator

Effect: If $<$op$>$ is given, \stmt{verti} vertically compiles the
	operator $<$op$>$.
	If no argument is given, the whole database \stmt{*rfi-database*} will
	be vertically compiled.

see also: \consult
