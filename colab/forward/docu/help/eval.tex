\eval:

Format: \stmt{eval}

Options: none

Effect: Semi-naive bottom-up evaluation is performed on the databases 
	\stmt{*up-rulebase*} and \stmt{*factbase*}. The derived facts are added to the 
	database \stmt{*derived-factbase*}.

see also: \stmb{magic-eval}, \stmb{magic-query}

