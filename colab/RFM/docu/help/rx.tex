\rx:

Format: \stmt{rx $<$clause$>$}

Options: $<$clause$>$ a \RELFUN{} clause

Effect: The $<$clause$>$ will be removed from the database \stmt{*rfi-database*}.

see also: \rxft, \rxhn, \consult, \destroy, \replace
