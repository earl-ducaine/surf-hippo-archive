\stmb{fw-transform}:

Format: \stmt{fw-transform}

Options: none.

Effect:	All rules of the database \stmt{*up-rulebase*} will be horizontally compiled 
	into the database \stmt{*forward-rules*}.

see also: \stmb{fw-compile}, \stmb{fw-compile-facts}, \stmb{fw-compile-rules}
