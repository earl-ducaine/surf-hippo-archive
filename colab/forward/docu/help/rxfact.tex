\rxfact:

Format: \stmt{rxfact $<$literal$>$}

Options: $<$literal$>$ the fact

Effect: The rule \stmt{(hn literal)} will be removed from the \stmt{*factbase*}.
        This command is an abbreviation of \stmt{rx (fact literal)}.

see also: \rx, \consult, \destroy, \replace
